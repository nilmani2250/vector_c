#include<stdio.h>
void main()
{
int i,c,num,num1;
printf("Enter starting and Ending range:\n");
scanf("%d %d",&num,&num1);

for(;num<=num1;num++)
{
for(i=1,c=0;i<=num;i++)
{
if(num%i===0)
c++;
}
if(c==2)
printf("%d is prime number\n",num);
}
}
